# Ultra-lightweight requirements for cloud deployment
# All packages use pre-built wheels only - no compilation required

# Core Framework
fastapi==0.104.1
uvicorn==0.24.0

# OpenAI Integration (optional)
openai==1.51.2

# Basic Document Processing (lightweight)
PyPDF2==3.0.1
python-docx==1.1.2

# Web & HTTP
python-multipart==0.0.6
httpx==0.25.2
requests==2.31.0

# Core Dependencies (stable versions with wheels)
pydantic==2.3.0
python-dotenv==1.0.0
BeautifulSoup4==4.12.2

# Build essentials
wheel
setuptools
