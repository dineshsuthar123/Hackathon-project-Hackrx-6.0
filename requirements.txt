# EMERGENCY LIGHTWEIGHT DEPLOYMENT - Render Memory Fix
# Core functionality only to prevent 502 errors

# Core FastAPI and server
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
pydantic>=2.5.3,<3.0.0

# HTTP client and file handling
httpx>=0.26.0
PyPDF2==3.0.1
python-multipart==0.0.6

# Minimal ML dependencies for basic functionality
numpy==1.24.3

# COMMENTED OUT - CAUSING MEMORY ISSUES ON RENDER:
# scikit-learn==1.3.2           # ~100MB 
# sentence-transformers==2.2.2  # ~500MB
# transformers>=4.35.0          # ~200MB  
# torch>=2.0.0                  # ~800MB
# accelerate>=0.20.0            # ~50MB
